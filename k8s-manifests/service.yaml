apiVersion: v1
kind: Service
metadata:
  name: endhunger-service
  namespace: endhunger
spec:
  type: NodePort
  selector:
    app: endhunger-web
  ports:
    - name: http
      port: 80
      targetPort: 80
      